# Exclude patterns — files/dirs excluded from hermes-pack push
# These are rsync-style patterns relative to ~/.hermes/
# One pattern per line. Comments start with #.

# Hermes runtime (reinstalled by `hermes update`)
hermes-agent/
bin/

# Caches
cache/
audio_cache/
image_cache/
provider_models_cache.json
models_dev_cache.json
ollama_cloud_models_cache.json

# Logs and sessions
logs/
sandboxes/
sessions/
pastes/

# Database temp files
*.db-shm
*.db-wal

# Runtime SQLite databases
state.db
state.db.gz
state.db.malformed-backup-*
state-snapshots/
verification_evidence.db
kanban.db
kanban/boards/*/kanban.db

# Runtime PID/state files
gateway.pid
gateway_state.json
processes.json
checkpoints/

# Lock and backup files
*.lock
*.bak.*

# IDE and editor settings (machine-specific)
.idea/

# UI state flags
.scratch_tip_shown

# Python cache
__pycache__/

# npm dependencies (regenerate from package-lock.json)
node_modules/

# OS files
.DS_Store

# Secrets and auth (never push these)
.env
auth.json

# hermes-pack internal
.hermes-pack-repo/
.hermes-pack-backup/
.hermes-pack.conf

# History
.hermes_history

# Skills
skills/.hub/
skills/.usage.json
skills/.curator_state
skills/.bundled_manifest
skills/.curator_backups/

# Misc temp/cache files
interrupt_debug.log
.skills_prompt_snapshot.json
.update_check
pairing/_rate_limits.json